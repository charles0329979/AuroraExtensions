# 皮皮喵当日仓库分类与导入结果（2026-08-28）

## 结果

- 连接设备完整抓取：440 条，较 2026-08-26 的 287 条增加 153 条净数量。
- 精确不变 259 条；新增或换址 181 条；旧记录消失 28 条；同名换域名 4 条。
- 两轮网络检测与设备页面功能检测后：可用 109、部分可用 165、失效 164、需要登录 2。
- 109 条可用记录中：漫画 98、轻小说 5、图库 3、动画 2、游戏 1。
- 非漫画的 11 条只保留在总清单，不进入 Aurora 漫画插件入口。

“可用”只代表皮皮喵中的列表或搜索入口能返回内容。Aurora 中只有存在真实解析实现、同证书 APK、仓库索引记录，并完成设备验证的来源才标为正式可用。

## 本地项目状态

- 正式验证：33 条皮皮喵可用记录；另有 1 条“如漫画”原记录已失效，但后继域名插件通过设备验证。
- 降级可用：3 条（古风漫画、漫圈子、360漫画）。
- 待设备验证：0 条。
- 仍需编写解析规则：62 条漫画源。
- 当前仓库：39 个签名一致的 APK；完整性校验通过。

本轮新增的包子漫画插件已把默认镜像切换到今天实测可用的 `https://cn.webmota.com`。设备验证覆盖热门列表、详情、3874 章目录和旧章节 13 页图片；最新章节可能被站点限制为官方 APP 专享，目录与历史章节仍可阅读。

喵趣漫画通过热门列表、详情、220 章目录和最新章节 22 页图片验证。Rawkuma 通过列表、详情、274 章目录和样本章节 22 页图片验证。

MYCOMIC 通过热门列表、详情、262 章目录和样本章节 30 页图片验证。香香腐宅已切换到当日主镜像 `https://boyloveheaven13.cc`，并保留 `https://boyloveheaven14.cc` 备用；设备通过列表、详情、141 章目录和最新章节 87 页图片验证。

Vomic 当日网页列表仍能展示内容，但旧公开 API 已返回 404，网页章节又要求登录，现有插件无法满足本地直接阅读标准。因此未计入正式源，其 APK 和图标已移入隔离目录，等待后续按新网页结构重写并处理登录限制。

天天看漫画为本项目新增的独立解析实现。规则在本地直接解密 MCCMS 的 AES 章节参数，不依赖 WebView 执行网页脚本；设备验证覆盖 20 部热门列表、作品详情、916 个目录项和最新章节 89 页图片。这套解密逻辑可继续复用于同类站点。

开心漫画为第二个本地实现的加密站点规则。新版页面把章节参数以 AES-CBC 加密，插件在设备本地解密并拼接图片服务器地址，不依赖 WebView；设备验证覆盖 20 部热门列表、作品详情、60 章目录和最新章节 122 页图片。

七夕漫画已完成列表、详情、目录及 AES 章节参数解析，设备可显示 20 部列表和样本作品 1061 章目录；但当前图片 CDN 对设备网络返回 Cloudflare 403，最新章节 30 张图片无法加载。因此解析代码予以保留，APK 不进入正式仓库，待找到可稳定切换的图片镜像后再复验。

思思漫画使用页面内 Base64 图片清单，不需要 WebView 或执行远程脚本。设备验证覆盖 20 部热门列表、作品详情、203 个目录项和最新第100话 16 页图片；图片 CDN 在设备网络中可直接读取。

漫画客已按站点的新列表、详情与目录模板重写，同时复用原站的章节图片解密算法。设备验证覆盖 20 部热门列表、作品详情、20 个首屏目录项和最新章节 69 页图片。

如漫画使用公开维护规则指向当前后继域名 `https://m.rumanhua2.com`。设备验证覆盖 20 部热门列表、作品详情、52 章完整目录和最新章节 69 页图片。

漫画大全保留皮皮喵记录中的原域名 `http://www.yueman1.cc`，插件自动识别 GB2312 页面，并在本地解码章节脚本中的 Base64 图片清单。设备验证覆盖 20 部最新作品、作品详情、样本 46 章目录和最新章节 228 张图片地址，正文图片可直接读取。

酷漫星（APP）在本次皮皮喵检测中只有入口可达，列表返回 0 条；其 `https://d9zfb53b.lstool.xyz` 端点当前不再提供可解析目录。为避免把空壳源放入用户仓库，本轮不生成 APK，待获得仍在工作的官方接口后再实现。

公开的漫画人扩展需要先由漫画人官方 APP 生成用户 ID/Token，不能独立工作，因此未加入可安装仓库；其 APK 和图标已移出仓库，保存在 `ppcat_device_inspect/quarantine_20260828` 以便后续重写网页规则时参考。

250漫画为本项目新增的独立网页解析实现。该站不要求登录，列表、详情、目录和正文图片均由服务端直接返回，不依赖 WebView、远程脚本或会话令牌。设备验证覆盖 37 个首页入口、中文关键词搜索、样本作品详情、808 章目录和最新章节 19 页正文图片；正文图片在平板阅读器中已实际渲染。

BikaBika漫画已完成独立插件、分页热门、最近更新、中文搜索、详情、重复目录去重和正文图片规则。设备首轮发现站点错误宣称支持 HTTP/2，插件已强制降为 HTTP/1.1；第二轮又补齐分类页与首页两套卡片模板。最终版已在平板通过热门列表、关键词搜索、作品详情、目录和正文图片全链路验证；样本作品共 1 章、正文 41 页，第 1 页已在阅读器中实际渲染，现计入正式健康源。

可漫画已完成独立网页插件，解析分页目录、首页最近更新、站内搜索、详情、章节和正文图片地址。站点无需登录，正文使用可直接访问的 WebP 图片，不依赖 WebView、加密脚本或短期令牌。平板验证覆盖 21 个首屏热门条目、数字关键词搜索、样本作品详情、18 章目录和最新卷 114 页正文；第 1 页已在阅读器中实际渲染。

360漫画已完成热门、最新、数字关键词搜索、详情、目录和正文图片插件，并同时适配站点的桌面版、移动版两套模板。平板验证覆盖 20 个首屏热门条目、搜索结果、样本作品详情和 670 个目录项；旧章节 36 页正文可实际阅读。站点最近若干章节只返回包子漫画 APP 推广图，因此本源进入仓库但标为“降级可用”，不作为完整健康源推荐。

36漫画已完成独立插件，支持热门、最近更新、真实站内搜索及搜索翻页、详情、完整目录和 AES-CBC 章节图片清单解密。平板验证覆盖 20 个首屏热门条目、ASCII 关键词搜索、样本作品详情、33 章完整目录和当天最新章节 60 页正文；第 1 页已在阅读器中实际渲染，没有登录、推广图或短期令牌限制，现计入正式健康源。

漫画456已完成独立插件，支持排行、分页更新、分页站内搜索、详情、多组章节目录以及新旧两代图片地址规则。旧章节直接使用完整图片 URL；当天新作则由插件按网页同等规则在本地组合图片服务器、章节目录和文件名。平板验证覆盖热门排行、`ONE` 关键词搜索、最近更新、样本作品详情、27 章完整目录和当天最新章节 38 页正文；第 1 页已实际渲染，现计入正式健康源。

搜漫原站是跨站索引，本身不保存正文图片；其结果混合腾讯、哔哩哔哩、动漫之家、已关闭站点和付费章节，不能直接等同为本地书源。项目因此实现“可读线路”：保留搜漫入口，只接入当日仍可完整读取的非常爱漫后端，并过滤付费、空目录和失效外链。插件支持热门、最近更新、站内搜索、详情、完整目录以及页面内 Base64 图片清单解码。平板验证覆盖 21 个热门条目、`TSUYOSHI` 搜索、20 个最近更新条目、样本作品 73 章和最新章节 33 页正文；第 1 页已在阅读器中实际渲染。

## 关键文件

- `catalog/ppcat-sources-20260828.csv`：440 条完整分级清单。
- `catalog/healthy-sources-20260828.json`：项目可读取的 109 条可用清单及实现状态。
- `catalog/healthy-sources-20260828.csv`：同内容的表格版本。
- `catalog/ppcat-comparison-20260826-to-20260828.json`：两次仓库差异。
- `scripts/import-ppcat-sources-20260828.ps1`：可重复导入脚本。
- `scripts/verify-repo-integrity.ps1`：APK 哈希、大小、签名和健康清单一致性校验。

## 下一批优先级

后续应从 62 条“规则待实现”漫画源中，优先选择今天设备能返回 18–20 个条目、无需登录且存在公开维护实现的来源。不要按网站数量批量生成空壳；每条必须依次通过列表、详情、章节、图片和域名切换五项验证。
